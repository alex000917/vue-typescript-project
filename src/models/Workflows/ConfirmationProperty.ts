import { BaseContent } from "../BaseContent";

export class ConfirmationProperty extends BaseContent {
    myspType = "ConfirmationProperty";
    specificConfirmationMessage: boolean = false;
    type: number = 1;
  }
  